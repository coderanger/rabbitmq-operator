
---
apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.2.5
  creationTimestamp: null
  name: rabbitvhosts.rabbitmq.coderanger.net
spec:
  group: rabbitmq.coderanger.net
  names:
    kind: RabbitVhost
    listKind: RabbitVhostList
    plural: rabbitvhosts
    singular: rabbitvhost
  scope: Namespaced
  validation:
    openAPIV3Schema:
      description: RabbitVhost is the Schema for the rabbitvhosts API
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
          type: string
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        spec:
          description: RabbitVhostSpec defines the desired state of RabbitVhost
          properties:
            connection:
              properties:
                host:
                  type: string
                insecureSkipVerify:
                  type: boolean
                passwordSecretRef:
                  properties:
                    key:
                      type: string
                    name:
                      type: string
                  required:
                  - name
                  type: object
                port:
                  type: integer
                protocol:
                  type: string
                username:
                  type: string
                vhost:
                  type: string
              type: object
            policies:
              additionalProperties:
                properties:
                  applyTo:
                    description: 'What this policy applies to: "queues", "exchanges",
                      etc.'
                    type: string
                  definition:
                    description: Additional arguments added to the entities (queues,
                      exchanges or both) that match a policy
                    type: string
                  pattern:
                    description: Regular expression pattern used to match queues and
                      exchanges, , e.g. "^ha\..+"
                    type: string
                  priority:
                    description: Numeric priority of this policy.
                    type: integer
                required:
                - definition
                - pattern
                type: object
              type: object
            skipUser:
              type: boolean
            vhostName:
              type: string
          type: object
        status:
          description: RabbitVhostStatus defines the observed state of RabbitVhost
          type: object
      type: object
  version: v1beta1
  versions:
  - name: v1beta1
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
